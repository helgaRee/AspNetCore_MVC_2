@model SignInViewModel

@{
    ViewData["Title"] = Model.Title;
}

<section id="signin">
    <div class="container">

        <form method="post" asp-controller="Auth" asp-action="SignIn" novalidate>
            <div asp-validation-summary="All"></div>
            
            <div>
                @Model.ErrorMessage;
            </div>

            <h1>Welcome Back</h1>
            <p>Don't have an account yet?<a asp-controller="Auth" asp-action="SignUp">Sign up here</a>.</p>

            <div class="content">

                <div id="form-email" class="input-group">
                    <label asp-for="Form.Email"></label>
                    <span asp-validation-for="Form.Email"></span>
                    <input asp-for="Form.Email" />
                </div>

                <div id="form-password" class="input-group">
                    <label asp-for="Form.Password"></label>
                    <span asp-validation-for="Form.Password"></span>
                    <input asp-for="Form.Password" />
                </div>

                <div id="form-remember" class="checkbox">
                    <div class="checkbox-group">
                        <input asp-for="Form.RememberMe" />
                        <label asp-for="Form.RememberMe"></label>
                    </div>
                </div>
                <button id="form-submit" class="btn-theme-s" type="submit">@Model.Title</button>

                <a id="form-forgot-password" asp-controller="Account" asp-action="Forgot">Forgot your password?</a>

            </div>

        </form>
    </div>
</section>
@* @model SignInViewModel

@{
    ViewData["Title"] = "Sign In to your account";
}


<section class="signin">
    <div class="container">

        @if (!string.IsNullOrEmpty(ViewData["ErrorMessage"]?.ToString()))
        {
            <div class="error-message">
                @ViewData["ErrorMessage"]
            </div>
        }


        <form method="post" asp-action="SignIn" asp-controller="Auth" novalidate>
            <h1>Sign in to your account</h1>
            <p>Doesn't have an account yet?' <a asp-action="SignUp" asp-controller="Auth">Sign up here</a></p>

            <div class="form-content">
                <div id="email" class="input-group">
                    <label asp-for="Form.Email"></label>
                    <input asp-for="Form.Email" />
                </div>
                <div id="password" class="input-group">
                    <label asp-for="Form.Password"></label>
                    <input asp-for="Form.Password" />
                </div>
                <div id="rememberMe" class="checkbox-group">
                    <div class="input-group">
                        <input asp-for="Form.RememberMe" />
                        <label asp-for="Form.RememberMe">
                            Keep me signed in.
                        </label>
                    </div>
                </div>

                <button id="btn-submit" type="submit" class="btn-theme-s">Sign In</button>

            </div>
        </form>

    </div>
</section> *@

